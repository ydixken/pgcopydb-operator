# Artifact Hub repository metadata. For an OCI chart repo this file is NOT
# part of the chart (see .helmignore); it is pushed next to the chart under
# the fixed `artifacthub.io` tag with oras.
#
# repositoryID is assigned by Artifact Hub when the repository is added there;
# set it and re-push for verified-publisher status.
# repositoryID: <assigned-by-artifact-hub>
owners:
  - name: Yannick Dixken
    email: ydixken@gmail.com
