{{- if and .Values.rbac.create .Values.leaderElection.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "pgcopydb-operator.fullname" . }}-leader-election
  labels:
    {{- include "pgcopydb-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "pgcopydb-operator.fullname" . }}-leader-election
subjects:
  - kind: ServiceAccount
    name: {{ include "pgcopydb-operator.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
